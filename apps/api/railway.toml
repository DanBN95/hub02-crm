[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm -F api build"

[deploy]
startCommand = "pnpm -F api start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
